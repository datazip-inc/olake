name: Integration Tests

on:
  push:
    branches:
      - "feat/testing_framework"
  pull_request:
    branches:
      - "*"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.23.2'
        
      # Create data directories with proper permissions
      - name: Create data directories
        run: |
          sudo mkdir -p ./writers/iceberg/local-test/data/{postgres-data,minio-data,ivy-cache}
          sudo chown -R 999:999 ./writers/iceberg/local-test/data
          sudo chmod -R 777 ./writers/iceberg/local-test/data

      # Start required containers
      - name: Start MySQL container
        run: |
          pushd ./drivers/mysql
          docker compose up -d
          popd
      - name: Wait for MySQL to be ready
        run: |
          for i in {1..30}; do
            if docker exec olake_mysql-test mysql -h localhost -u root -proot1234 -e "SELECT 1" &> /dev/null; then
              echo "MySQL is up!"
              break
            fi
            echo "Waiting for MySQL to be ready..."
            sleep 5
          done
      - name : Start PostgreSQL container
        run: |
          pushd ./drivers/postgres
          docker compose up -d
          popd
      - name: Wait for PostgreSQL to be ready
        run: |
          for i in {1..30}; do
            if docker exec olake_postgres-test psql -h localhost -U postgres -d postgres -c "SELECT 1" &> /dev/null; then
              echo "PostgreSQL is up!"
              break
            fi
            echo "Waiting for PostgreSQL to be ready..."
            sleep 5
          done

      - name: Start Iceberg services
        run: |
          pushd ./writers/iceberg/local-test
          docker compose up -d
          popd
    
      # Wait for services to be ready
      - name: Wait for services to be ready
        run: sleep 60
      
      # Install go dependencies and build
      - name: Install dependencies
        run: go mod download

      - name: Build
        run: go build -v ./...

      # Build Maven project
      - name: Run Maven build
        run: |
          cd ./writers/iceberg/debezium-server-iceberg-sink
          mvn clean package -DskipTests

      # Run the integration tests
      - name: Run integration tests for MySQL
        run: |
          go test -v ./drivers/mysql/internal/... -tags=integration --timeout 0
      
      - name: Run integration tests for PostgreSQL
        run: |
          go test -v ./drivers/postgres/internal/... -tags=integration --timeout 0
    
          
      # Clean up containers when done
      - name: Clean up Docker containers
        if: always()
        run: |
          pushd ./writers/iceberg/local-test
          docker compose down
          popd
          
          pushd ./drivers/mysql
          docker compose down
          popd