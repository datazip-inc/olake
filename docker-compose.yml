version: '3.8'

services:
  mongodb_sync:
    image: olake/source-mongodb:dev
    command: >
      sync
      --config /mnt/config/config.json
      --catalog /mnt/config/catalog.json
      --destination /mnt/config/writer.json
    volumes:
      - ./drivers/mongodb/examples:/mnt/config